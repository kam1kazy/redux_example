/// -------------------------------
/// ACTION - функция (объект) которая возвращает объект
/// -------------------------------

export const setName = (name) => ({
  type: "ADD_NAME",
  payload: name
});
